<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Message Board</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
      <h1>Whatup World</h1>
      <hr>
      <form action="/message" method="post">
        <input placeholder="Your Name" type="text" name="name"><p></p>
        <textarea placeholder="Your Message" name="message" cols="40" rows="6" ></textarea><p></p>
        <input id="post" type="submit" value="Post Message">
      </form>
      <% if(typeof(errors) != 'undefined' ) { %>
        <p>Errors</p>
        <% for (var x in errors) { %>
          <li style="color:red"><%= errors[x].message %></li>
        <% } %>
        <a href="/">Go Back</a>
      <% } else { %>
        <hr>
        <% for(x in messages) { %>
          <h3>"<%= messages[x].message %>" - <%= messages[x].name %></h3>
          <% for (var y = 0; y < messages[x].comments.length; y++) { %>
            <p>"<%=messages[x].comments[y].comment %>" - <%= messages[x].comments[y].name %></p>
          <% } %>
          <form action="/comment/<%=messages[x]._id %>" method="post">
            <input placeholder="Your Name" type="text" name="name"><p></p>
            <textarea placeholder="Your Comment" name="comment" cols="40" rows="3" ></textarea><p></p>
            <input id="post" type="submit" value="Post Comment">
          </form>
        <% } %>
      <% } %>
      <hr>
    </div>
  </body>
</html>
